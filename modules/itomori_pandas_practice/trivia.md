Data structures in pandas wrap around `numpy.ndarray`.

`Column` is an extension of the `Series` data structure (both are from pandas).

`DataFrame` is not immutable.

`DataFrame` comes from the `data.frame` in R
(pandas in general is trying to replicate R-like statistical computing for Python).